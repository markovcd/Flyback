No release key yet.

Replace this whole file with the public half of the release signing key (the
text from -----BEGIN PUBLIC KEY----- to -----END PUBLIC KEY-----). Until then a
build installs no updates at all. See docs/adr/0088 for how the key is made and
where its private half is kept.
