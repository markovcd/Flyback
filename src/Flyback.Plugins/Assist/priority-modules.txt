# Modules whose descriptions the assistant is always given.
#
# When every module's description would run past the budget set under
# Settings → Assistant, these keep theirs, the rest keep theirs in module-list order
# for as long as there is room, and every one left out is marked on the canvas.
# The assistant can still look up any module it was not told about.
#
# One type id a line. A line starting with # is a note, and an id no loaded
# module has is passed over. This file is written once and never overwritten:
# delete it to have the list Flyback ships with put back at the next start.

# What every patch is made of.
output
time
coord
value
audio.note
audio.quantiser
audio.tune
audio.sample
picture
midi.in

osc.sine
osc.saw
osc.triangle
osc.square
osc.pulse
osc.string

seq.tempo
seq.hold
seq.notes
seq.values
env.adsr
audio.duck
audio.filter
audio.noise
audio.slew
audio.drive
audio.delay
audio.reverb

math.expression
math.clamp
math.mix
math.mixer
math.desk
math.smoothstep
math.remap

space.rotate
space.scale
space.translate
space.transform
space.polar
space.tile
space.mirror
space.kaleidoscope
space.warp

pattern.clouds
pattern.checker
pattern.rings

color.rgb
color.hsv
color.split
color.mix
color.gain
color.ink
color.vignette

feedback
feedback.trails

# From the plugins Flyback ships with: the voice of nearly every track, and the
# shapes most pictures start from.
flyback.voice.bell
flyback.voice.decay
flyback.voice.drum
flyback.voice.euclid
flyback.voice.fade
flyback.voice.hiss
flyback.voice.osc
flyback.voice.stroke
flyback.voice.wander

flyback.effects.chorus
flyback.effects.echo

flyback.picture.box
flyback.picture.cells
flyback.picture.circle
flyback.picture.combine
flyback.picture.fill
flyback.picture.fractal
flyback.picture.grade
flyback.picture.layer
flyback.picture.palette
flyback.picture.polygon
flyback.picture.posterise
flyback.picture.star
